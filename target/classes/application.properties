spring.application.name=eagleshare

# The address of your MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/eagle_share

spring.datasource.username=root
spring.datasource.password=Eagle_Share26!?

# Tells Hibernate (the translator) to update your tables if you change Java code
spring.jpa.hibernate.ddl-auto=update

# Shows the actual SQL commands in your console (good for learning!)
spring.jpa.show-sql=true

# Set the maximum file size to 10MB
spring.servlet.multipart.max-file-size=10MB

# Set the maximum request size to 10MB
spring.servlet.multipart.max-request-size=10MB